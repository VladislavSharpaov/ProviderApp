<Page x:Class="ProviderApp.Pages.RequestPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProviderApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RequestPage"      
      FontFamily="{StaticResource Disket Mono}"
      FontSize="14"
      IsHitTestVisibleChanged="Page_IsHitTestVisibleChanged"
      Loaded="Page_Loaded">


    <Page.Resources>
        <Style TargetType="{x:Type Button}"
               BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Height"
                    Value="30"/>
            <Setter Property="Width"
                    Value="30"/>
            <Setter Property="Margin"
                    Value="0"/>
        </Style>
        <Style TargetType="{x:Type TextBox}"
               BasedOn="{StaticResource BaseTextBoxStyle}">
            <Setter Property="Height"
                    Value="30"/>
            <Setter Property="Width"
                    Value="200"/>
        </Style>
        <Style TargetType="{x:Type ComboBox}"
               >
            <Setter Property="Height"
                    Value="30"/>
            <Setter Property="Width"
                    Value="200"/>
            <Setter Property="Margin"
                    Value="20 0"/>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Background="WhiteSmoke"
                />
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    Margin="10"
                    Orientation="Horizontal">
            <Button Name="AddRequestButton"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Content="+" 
                    Click="AddRequestButton_Click"/>
            <TextBlock Text="Заявки"
                       VerticalAlignment="Center"
                       Margin="10 0"
                       FontSize="18"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <TextBox Name="SearchTextBox"
                     TextChanged="SearchTextBox_TextChanged"
                     FontSize="14"
                     Margin="20 0"/>

        </StackPanel>
        <ListBox Grid.Row="1"
                 Name="RequestListBox">
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate>
                                <Grid Margin="3"
                                      Background="WhiteSmoke">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal">

                                        <StackPanel Margin="10"
                                                    Width="200">
                                            <TextBlock Text="Номер договора:"/>
                                            <TextBlock Text="{Binding ContractID}"/>
                                        </StackPanel>
                                        <StackPanel Margin="10"
                                                    Width="200">
                                            <TextBlock Text="Сотрудник:"/>
                                            <TextBlock Text="{Binding Worker.Surname}"/>
                                        </StackPanel>
                                        
                                        <StackPanel Margin="10"
                                                    Width="100">
                                            <TextBlock Text="Статус:"/>
                                            <TextBlock Text="{Binding Status}"/>
                                        </StackPanel>
                                        <StackPanel Margin="10"
                                                    Width="200">
                                            <TextBlock Text="Дата:"/>
                                            <TextBlock Text="{Binding Date, StringFormat='dd.MM.yyyy'}"/>
                                        </StackPanel>
                                        <StackPanel Margin="10"
                                                    Width="300">
                                            <TextBlock Text="Услуга:"/>
                                            <TextBlock Text="{Binding Service.Name}"/>
                                        </StackPanel>
                                        

                                    </StackPanel>
                                    <Button 
                                            Name="ChangeRequestButton"
                                            Click="ChangeRequestButton_Click"
                                            Grid.Column="1"
                                            Margin="20 0"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Right">
                                        <Path Data="{StaticResource ChangePathData}"
                                              Fill="Black"
                                              Stretch="UniformToFill"
                                              Margin="5"></Path>
                                    </Button>

                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</Page>
